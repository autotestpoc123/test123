  改动汇总

  5 个文件，共 6 处变更

  analysis_task.config.psd1
  - 新增 LogRoot = 'C:\SysAdmin\log'，与已有 InputRoot 风格一致

  Invoke-AuditLog.ps1
  - $resolvedOutputRoot 改为四级优先链：-OutputRoot 参数 > WECOM_AUDIT_LOG_ROOT 环境变量 > config.LogRoot > 原 fallback；子目录 wecom_audit_log 由脚本自动追加
  - 任务成功后（if (-not $?) 通过之后、taskResults.Add(completed) 之前）插入原始文件删除：先确认 $backupTaskInputPath 存在再删，缺备份时写 WARNING 日志但不删、不中断流程

  Invoke-AuditValidate.ps1
  - $resolvedOutputRoot 同步应用相同的四级优先链，确保校验脚本找到同一路径下的 runs 目录

  Setup_DeviceAnalysis_Env.ps1
  - 移除调用 wecom_devicelog_analysis.ps1 时传递的 -DeleteInputAfterAnalysis 参数（临时 CSV 的清理已由 finally 块负责，原始 XLSX 的清理统一归 Invoke-AuditLog.ps1）

  wecom_devicelog_analysis.ps1
  - 删除 [switch]$DeleteInputAfterAnalysis 参数声明
  - 删除两处 if ($DeleteInputAfterAnalysis) { Remove-Item ... } 代码块
