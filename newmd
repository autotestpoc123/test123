下面按 Jira item 的格式，把“员工照片显示”新增功能拆成可实现、可验收的前端需求。范围只包含“展示员工头像/照片”，不包含“基于照片搜索员工”。

Epic: 员工照片展示能力
目标：在员工详情页、搜索结果列表、导航栏当前用户区、组织结构项中统一展示员工照片；无照片或加载失败时优雅降级为 initials 占位。

JIRA-001: 新增员工头像展示组件 user-avatar
类型：Story
优先级：P0
预估：1d

描述：
创建可复用头像组件，用于统一展示员工照片，并在无照片时显示姓名 initials 占位。

验收标准：

支持输入 msid、fullName、size、shape。
有照片时显示真实头像。
无照片、无 msid、接口返回空时显示 initials。
支持 sm、md、lg 三种尺寸。
支持圆形头像样式。
不在业务页面中重复写 <img> 和 fallback 逻辑。
涉及文件：

src/app/components/common/user-avatar/user-avatar.component.ts
src/app/components/common/user-avatar/user-avatar.component.html
src/app/components/common/user-avatar/user-avatar.component.css
JIRA-002: 新增员工照片请求服务 UserPhotoService
类型：Story
优先级：P0
预估：1d

描述：
新增服务，根据员工 MSID 请求后端照片接口，并返回可用于 <img> 展示的安全 URL。

验收标准：

通过 GET /api/v1.0/photos/{msid} 获取照片。
请求使用 responseType: 'blob'。
对非法或空 msid 不发起 HTTP 请求。
204/404/403 时返回 null，不抛出用户可见错误。
5xx 或网络错误时静默降级，并保留开发侧可观测日志。
相同 MSID 的并发请求只触发一次 HTTP 请求。
涉及文件：

src/app/service/user-photo.service.ts
src/app/data/constants.ts
JIRA-003: 增加头像请求缓存与去重
类型：Task
优先级：P0
预估：0.8d

描述：
为员工照片请求增加内存缓存，避免同一员工照片在多个页面位置重复请求。

验收标准：

使用 MSID 作为缓存 key。
同一个 MSID 多次调用复用同一个请求结果。
缓存有最大容量限制。
缓存命中时不重复发起 HTTP 请求。
invalidate(msid) 可清除指定员工头像缓存。
涉及文件：

src/app/service/user-photo.service.ts
JIRA-004: 实现头像懒加载
类型：Task
优先级：P0
预估：0.8d

描述：
头像组件进入视口后再加载照片，降低列表和组织结构页面的初始请求压力。

验收标准：

使用 IntersectionObserver 监听头像组件是否进入视口。
进入视口后才调用 UserPhotoService.getPhoto()。
触发加载后断开 observer。
浏览器不支持 IntersectionObserver 时自动退化为立即加载。
滚动长列表时不会一次性请求所有头像。
涉及文件：

src/app/components/common/user-avatar/user-avatar.component.ts
JIRA-005: 在员工详情页展示员工大头像
类型：Story
优先级：P0
预估：0.5d

描述：
在员工详情页头部展示当前员工照片，替换原有默认用户图标或空白占位。

验收标准：

员工详情页顶部显示 lg 尺寸头像。
使用当前员工 UserAccount.MSID 请求照片。
使用当前员工 UserAccount.FullName 生成 fallback initials。
无照片时页面布局不抖动。
点击或切换员工详情时头像正确更新。
涉及文件：

src/app/components/user-view/user-view.component.html
JIRA-006: 在用户列表中展示员工小头像
类型：Story
优先级：P0
预估：0.5d

描述：
在搜索结果、历史浏览人员列表等用户表格中，在姓名前展示员工小头像。

验收标准：

user-table 中每行姓名前显示 sm 头像。
使用行数据中的 MSID 和 FullName。
头像加载失败不影响表格点击、筛选、分页。
列表滚动时头像懒加载生效。
表格导出逻辑不受头像影响。
涉及文件：

src/app/components/common/user-table/user-table.component.html
JIRA-007: 在导航栏当前登录用户区展示头像
类型：Story
优先级：P1
预估：0.5d

描述：
在页面右上角当前登录用户姓名前展示员工头像，增强用户身份识别。

验收标准：

导航栏当前用户区域显示 sm 头像。
使用登录用户 UserAccount.MSID 和 FullName。
登录用户信息未加载完成时不报错。
无头像时显示 initials。
不影响原有用户详情页跳转链接。
涉及文件：

src/app/components/common/navigation-bar/navigation-bar.component.html
JIRA-008: 在组织结构人员项中展示头像
类型：Story
优先级：P1
预估：0.5d

描述：
在组织结构、manager/direct reports 等人员项中显示员工头像。

验收标准：

组织结构人员项姓名前显示 sm 头像。
使用人员项中的 MSID 和 FullName。
不影响原有人员详情页跳转。
无头像时显示 initials。
多人员列表中懒加载生效。
涉及文件：

src/app/components/common/org-user-item/org-user-item.component.html
JIRA-009: 优化照片请求拦截器行为
类型：Task
优先级：P0
预估：0.8d

描述：
调整全局 HTTP 拦截器对照片请求的处理，避免照片请求触发全局 loading 抖动，并允许浏览器/服务端缓存策略生效。

验收标准：

/photos/ 请求不追加 Cache-Control: no-cache。
/photos/ 请求不触发全局 loading 计数。
/photos/ 请求错误不弹全局 toast。
普通 GET 请求仍保持原有 no-cache 行为。
普通业务请求 loading 行为不回归。
涉及文件：

src/app/interceptor/custom-request-interceptor.service.ts
JIRA-010: 修复 Interceptor loading debounce 实现
类型：Bug
优先级：P0
预估：0.5d

描述：
修复 CustomInterceptor 中 debounce 函数每次请求都重新创建的问题，保证 loading 状态更新节流生效。

验收标准：

debounce 包装函数只初始化一次。
setLoadingStatus() 不重复创建 debounce。
连续请求时 loading 状态更新稳定。
不影响现有错误处理逻辑。
涉及文件：

src/app/interceptor/custom-request-interceptor.service.ts
JIRA-011: 管理 Blob URL 生命周期
类型：Task
优先级：P0
预估：1d

描述：
为 URL.createObjectURL() 创建的头像 URL 增加受控释放策略，降低长时间使用时的内存占用风险。

验收标准：

成功加载照片后记录对应 object URL。
invalidate(msid) 时释放指定头像 URL。
页面卸载或服务销毁时统一释放已缓存 URL。
LRU 淘汰时不直接 revoke，避免可见图片变 broken。
释放逻辑不影响当前头像正常显示。
涉及文件：

src/app/service/user-photo.service.ts
JIRA-012: 增加头像服务单元测试
类型：Test
优先级：P1
预估：1d

描述：
为 UserPhotoService 增加单元测试，覆盖照片请求、缓存、异常降级、URL 释放等关键路径。

验收标准：

覆盖空 msid 不发请求。
覆盖非法 msid 不发请求。
覆盖 204 返回 null。
覆盖 404/403 静默降级。
覆盖成功 blob 转 URL。
覆盖相同 MSID 请求去重。
覆盖 invalidate(msid) 后重新请求。
覆盖 5xx 可观测日志与负缓存清理。
涉及文件：

src/app/service/user-photo.service.spec.ts
JIRA-013: 增加头像组件单元测试
类型：Test
优先级：P1
预估：0.8d

描述：
为 user-avatar 组件增加单元测试，覆盖 initials、fallback、懒加载和 msid 变化。

验收标准：

正确根据姓名生成 initials。
无姓名时显示 ?。
单词姓名可正常生成 initials。
背景色生成稳定。
不支持 IntersectionObserver 时立即加载。
msid 变化后重新加载头像。
涉及文件：

src/app/components/common/user-avatar/user-avatar.component.spec.ts
JIRA-014: 清理调试日志
类型：Task
优先级：P0
预估：0.2d

描述：
移除常量文件中的环境配置调试输出，避免生产环境控制台噪声。

验收标准：

删除 console.log(environment)。
不影响 API URL 常量生成。
构建行为不变。
涉及文件：

src/app/data/constants.ts
JIRA-015: 更新头像功能技术说明文档
类型：Task
优先级：P2
预估：0.5d

描述：
同步更新员工头像展示功能设计与实现说明，记录当前实现边界、缓存策略、错误处理和后续风险。

验收标准：

明确当前功能只包含头像展示，不包含照片检索。
说明 UserPhotoService、user-avatar、Interceptor 的职责。
记录 Blob URL 释放策略。
记录已知残余风险：LRU 淘汰不 revoke。
文档与代码实现一致。
涉及文件：

src/user_photo_feature.md
JIRA-016: 手工联调验证头像展示入口
类型：Test
优先级：P1
预估：0.7d

描述：
对所有头像展示入口进行手工验证，确保功能在真实页面中表现稳定。

验收标准：

员工详情页头像正常显示。
搜索结果用户列表头像正常显示。
导航栏当前用户头像正常显示。
组织结构人员项头像正常显示。
无照片用户显示 initials。
头像接口 404/204/5xx 时页面不报错。
大量头像加载时全局 loading 不闪烁。
浏览器 Network 中同一 MSID 不重复请求。
涉及页面：

/user
/user/:id
/search/:filter
导航栏搜索结果
组织结构区域


下面是一份可直接给 QA server 执行的 runbook。脚本名应为 Invoke-WeComAuditScheduler.ps1，你写的 Aduit 应该只是 typo。

目标运行方式

QA 建议部署成两个 Windows Task Scheduler jobs：

Phase Analysis: 先跑分析，生成 RunId 和 latest-run.json
运维窗口：补齐需要人工放入 source folder 的文件
Phase Validate: 读取上一步 RunId，做 validation + archive，失败时发邮件
不建议 QA/生产用 -Phase All，因为中间没有人工补文件窗口。

1. 部署代码到 QA Server

建议目录：

C:\SysAdmin\wecom_audit\
把项目文件复制过去，至少包含：

Invoke-WeComAuditScheduler.ps1
Invoke-AuditLog.ps1
Invoke-AuditValidate.ps1
wecom_analysis_comm.psm1
analysis_task.config.psd1
modules\
*.ps1 analysis dependencies
如果文件来自 zip 或网络共享，先 unblock：

Get-ChildItem C:\SysAdmin\wecom_audit -Recurse | Unblock-File
2. 准备 QA 目录和权限

当前 config 默认：

InputRoot  = 'C:\addin_deploy_cert'
LogRoot    = 'C:\SysAdmin\log'
BackupRoot = '\\cod.test.com.cn\apptest\wecom_audit_log_backup'
需要确保运行账号有权限：

New-Item -ItemType Directory -Force C:\addin_deploy_cert\wecom_audit_log
New-Item -ItemType Directory -Force C:\SysAdmin\log
并确认账号能访问 backup share：

Test-Path '\\cod.test.com.cn\apptest\wecom_audit_log_backup'
输入文件应放在：

C:\addin_deploy_cert\wecom_audit_log\
运行输出一般在：

C:\SysAdmin\log\runs\<RunId>\
C:\SysAdmin\log\latest-run.json
C:\SysAdmin\log\runs\<RunId>\validation\
3. 检查 QA config

打开：

C:\SysAdmin\wecom_audit\analysis_task.config.psd1
重点确认：

ScheduleAnchor = '20260402'
InputRoot      = 'C:\addin_deploy_cert'
LogRoot        = 'C:\SysAdmin\log'
BackupRoot     = '\\cod.test.com.cn\apptest\wecom_audit_log_backup'
Notification.QA
Tasks
BackupValidationRules
如果 QA server 要用独立配置，建议新建：

analysis_task.config.qa.psd1
然后运行时显式传：

-ConfigPath C:\SysAdmin\wecom_audit\analysis_task.config.qa.psd1
4. 检查邮件证书

config 里 QA 使用：

CertName = 'wecom-audit-qa-cert'
运行账号必须能读取该证书。建议先在 QA server 上用同一个账号测试：

cd C:\SysAdmin\wecom_audit
Import-Module .\wecom_analysis_comm.psm1 -Force
Get-Cert -KeyName 'wecom-audit-qa-cert'
如果返回为空或报错，preflight/validation/archive failure notification 会失败或写 notification sidecar。

5. 手动试跑 Phase Analysis

先准备 Analysis 阶段需要的文件。按当前 config，2-week cycle 需要类似：

C:\addin_deploy_cert\wecom_audit_log\MSMS WeCom Mail Log_{startDate}_{endDate}.csv
C:\addin_deploy_cert\wecom_audit_log\<member operation records xlsx>
然后执行：

cd C:\SysAdmin\wecom_audit

.\Invoke-WeComAuditScheduler.ps1 `
    -Phase Analysis `
    -env QA `
    -ConfigPath .\analysis_task.config.psd1
成功后检查：

Get-Content C:\SysAdmin\log\latest-run.json -Raw
如果 preflight 失败，exit code 是 3，检查：

C:\SysAdmin\log\runs\preflight_yyyyMMdd_HHmmss\preflight-report.json
C:\SysAdmin\log\runs\latest-preflight.json
6. 手动试跑 Phase Validate

Analysis 成功后，补齐 Validate 阶段要求的人工文件到 source folder：

C:\addin_deploy_cert\wecom_audit_log\
例如当前 config 里：

COD WeCom Login to Non-Approved Devices FID BU - Report({startDate} - {endDate}).msg
Conduct WeCom Log Audit file uploaded.msg    # 4-week cycle
然后执行：

cd C:\SysAdmin\wecom_audit

.\Invoke-WeComAuditScheduler.ps1 `
    -Phase Validate `
    -env QA `
    -ConfigPath .\analysis_task.config.psd1
Validate 会读取 latest-run.json，并校验日期一致后调用 Invoke-AuditValidate.ps1 -RunId <RunId>。

Exit code 含义：

0 = 成功
1 = validation missing/unexpected files
2 = archive/copy/delete 阶段失败
3 = preflight failed
其他 = analysis 或脚本异常
失败时重点看：

C:\SysAdmin\log\runs\<RunId>\validation\backup-validation-summary.json
C:\SysAdmin\log\runs\<RunId>\validation\backup-validation-report.txt
C:\SysAdmin\log\runs\<RunId>\validation\notification-failure.json
7. 创建 Scheduler Jobs

建议用运行账号登录 QA server，以管理员 PowerShell 执行。下面示例是每两周周四：

Analysis job: 08:00
Validate job: 16:00

请把 $User 改成实际 domain service account。

$ScriptRoot = 'C:\SysAdmin\wecom_audit'
$ConfigPath = 'C:\SysAdmin\wecom_audit\analysis_task.config.psd1'
$User = 'DOMAIN\svc-wecom-audit'

$ActionAnalysis = New-ScheduledTaskAction `
    -Execute 'powershell.exe' `
    -Argument "-NoProfile -ExecutionPolicy Bypass -File `"$ScriptRoot\Invoke-WeComAuditScheduler.ps1`" -Phase Analysis -env QA -ConfigPath `"$ConfigPath`"" `
    -WorkingDirectory $ScriptRoot

$TriggerAnalysis = New-ScheduledTaskTrigger `
    -Weekly `
    -WeeksInterval 2 `
    -DaysOfWeek Thursday `
    -At 08:00

Register-ScheduledTask `
    -TaskName 'WeCom-Audit-QA-Phase1-Analysis' `
    -Action $ActionAnalysis `
    -Trigger $TriggerAnalysis `
    -User $User `
    -RunLevel Highest `
    -Description 'WeCom audit QA phase 1 analysis'
再创建 Validate job：

$ActionValidate = New-ScheduledTaskAction `
    -Execute 'powershell.exe' `
    -Argument "-NoProfile -ExecutionPolicy Bypass -File `"$ScriptRoot\Invoke-WeComAuditScheduler.ps1`" -Phase Validate -env QA -ConfigPath `"$ConfigPath`"" `
    -WorkingDirectory $ScriptRoot

$TriggerValidate = New-ScheduledTaskTrigger `
    -Weekly `
    -WeeksInterval 2 `
    -DaysOfWeek Thursday `
    -At 16:00

Register-ScheduledTask `
    -TaskName 'WeCom-Audit-QA-Phase2-Validate' `
    -Action $ActionValidate `
    -Trigger $TriggerValidate `
    -User $User `
    -RunLevel Highest `
    -Description 'WeCom audit QA phase 2 validation and archive'
注册时系统会提示输入 $User 密码。

8. Scheduler Job 设置建议

在 Task Scheduler UI 里确认：

General:
- Run whether user is logged on or not
- Run with highest privileges

Conditions:
- 取消 Start the task only if the computer is on AC power，除非服务器策略要求

Settings:
- Allow task to be run on demand
- Stop the task if it runs longer than: 4 hours
- If the running task does not end when requested, force it to stop
- If the task is already running: Do not start a new instance
9. 手动触发验证 Scheduler

Start-ScheduledTask -TaskName 'WeCom-Audit-QA-Phase1-Analysis'
Get-ScheduledTaskInfo -TaskName 'WeCom-Audit-QA-Phase1-Analysis'

Start-ScheduledTask -TaskName 'WeCom-Audit-QA-Phase2-Validate'
Get-ScheduledTaskInfo -TaskName 'WeCom-Audit-QA-Phase2-Validate'
查看 LastTaskResult：

(Get-ScheduledTaskInfo -TaskName 'WeCom-Audit-QA-Phase1-Analysis').LastTaskResult
(Get-ScheduledTaskInfo -TaskName 'WeCom-Audit-QA-Phase2-Validate').LastTaskResult
0 表示成功。1/2/3 对应上面的业务 exit code。

10. Backfill 手动运行

如果要补跑历史日期，两个 phase 必须使用同一个 -StartDate，必要时也固定 -ForceCurrentRunWeeks：

.\Invoke-WeComAuditScheduler.ps1 `
    -Phase Analysis `
    -StartDate 20260319 `
    -ForceCurrentRunWeeks 4 `
    -env QA `
    -ConfigPath .\analysis_task.config.psd1

.\Invoke-WeComAuditScheduler.ps1 `
    -Phase Validate `
    -StartDate 20260319 `
    -ForceCurrentRunWeeks 4 `
    -env QA `
    -ConfigPath .\analysis_task.config.psd1
否则 Validate 会因为 handoff 日期不一致而中止，这是设计行为。
